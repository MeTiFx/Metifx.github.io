<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مهدی عرفانیان | Metifx | تحلیل‌گر ارشد XAUUSD و بنیان‌گذار استراتژی MRN</title>
    <meta name="description" content="مهدی عرفانیان، تحلیل‌گر ارشد بازار طلا (XAUUSD) و بنیان‌گذار استراتژی MRN. همکاری با پراپ‌فرم‌های بین‌المللی، منتورینگ تریدرهای حرفه‌ای و تحلیل تخصصی ساختار بازار.">
    <meta name="keywords" content="مهدی عرفانیان, Metifx, MRN Strategy, XAUUSD, تحلیل طلا, منتور فارکس, پراپ فرم">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="https://metifx.com/">

    <meta property="og:type" content="website">
    <meta property="og:title" content="مهدی عرفانیان | Metifx | استراتژی MRN">
    <meta property="og:description" content="تحلیل‌گر ارشد بازار طلا (XAUUSD) و بنیان‌گذار استراتژی MRN. همکاری با پراپ‌فرم‌های بین‌المللی و منتورینگ تریدرهای حرفه‌ای.">
    <meta property="og:image" content="https://metifx.com/og-cover.jpg">
    <meta property="og:locale" content="fa_IR">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="مهدی عرفانیان | Metifx">
    <meta name="twitter:description" content="تحلیل‌گر ارشد بازار طلا (XAUUSD) و بنیان‌گذار استراتژی MRN.">

    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' rx='22' fill='%23070a0f'/%3E%3Ctext x='50' y='68' font-size='58' font-family='Arial, sans-serif' font-weight='900' fill='%23d4af37' text-anchor='middle'%3EM%3C/text%3E%3C/svg%3E">

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "مهدی عرفانیان",
      "alternateName": "Metifx",
      "jobTitle": "تحلیل‌گر ارشد بازار طلا و بنیان‌گذار استراتژی MRN",
      "birthPlace": "خوی، ایران",
      "url": "https://metifx.com",
      "sameAs": ["https://t.me/MeTiTr", "https://www.instagram.com/mahdi_erfaniannn"]
    }
    </script>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        /* ============================================================
                   RESET & ROOT
                ============================================================ */
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --gold: #d4af37;
            --gold-light: #f5d97b;
            --gold-dark: #b8942a;
            --gold-glow: rgba(212, 175, 55, 0.25);
            --dark-bg: #070a0f;
            --dark-card: #0e121b;
            --dark-surface: #161c2b;
            --dark-border: rgba(255, 255, 255, 0.04);
            --text-primary: #f0f4fa;
            --text-secondary: #8895aa;
            --text-muted: #5a6a7e;
            --font: 'Plus Jakarta Sans', sans-serif;
            --radius: 20px;
            --transition: 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            --shadow-gold: 0 8px 40px rgba(212, 175, 55, 0.15);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font);
            background: var(--dark-bg);
            color: var(--text-primary);
            line-height: 1.7;
            overflow-x: hidden;
        }

        a {
            text-decoration: none;
            color: inherit;
        }
        ul {
            list-style: none;
        }
        img {
            max-width: 100%;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 28px;
        }

        ::-webkit-scrollbar {
            width: 6px;
        }
        ::-webkit-scrollbar-track {
            background: var(--dark-bg);
        }
        ::-webkit-scrollbar-thumb {
            background: var(--gold);
            border-radius: 20px;
        }

        /* ============================================================
                   BUTTONS
                ============================================================ */
        .btn-gold {
            background: linear-gradient(135deg, var(--gold), var(--gold-dark));
            color: #0a0d12;
            padding: 14px 38px;
            border-radius: 60px;
            font-weight: 700;
            font-size: 0.9rem;
            border: none;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            box-shadow: var(--shadow-gold);
            position: relative;
            overflow: hidden;
        }
        .btn-gold::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent, rgba(255, 255, 255, 0.2));
            opacity: 0;
            transition: var(--transition);
        }
        .btn-gold:hover::after {
            opacity: 1;
        }
        .btn-gold:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 12px 50px rgba(212, 175, 55, 0.35);
        }

        .btn-outline {
            background: transparent;
            border: 1.5px solid rgba(212, 175, 55, 0.4);
            color: var(--gold);
            padding: 12px 34px;
            border-radius: 60px;
            font-weight: 600;
            font-size: 0.9rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            backdrop-filter: blur(4px);
        }
        .btn-outline:hover {
            background: rgba(212, 175, 55, 0.08);
            border-color: var(--gold);
            transform: translateY(-2px);
            box-shadow: 0 0 40px rgba(212, 175, 55, 0.05);
        }

        /* ============================================================
                   HEADER — Glassmorphism
                ============================================================ */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            padding: 12px 0;
            background: rgba(7, 10, 15, 0.55);
            backdrop-filter: blur(24px) saturate(1.2);
            border-bottom: 1px solid rgba(212, 175, 55, 0.06);
            transition: var(--transition);
        }

        header.scrolled {
            background: rgba(7, 10, 15, 0.88);
            backdrop-filter: blur(32px) saturate(1.4);
            border-bottom-color: rgba(212, 175, 55, 0.12);
            box-shadow: 0 8px 60px rgba(0, 0, 0, 0.5);
        }

        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: -0.5px;
        }
        .logo .gold {
            color: var(--gold);
        }
        .logo .sub {
            font-size: 0.55rem;
            font-weight: 400;
            color: var(--text-secondary);
            letter-spacing: 2px;
            display: block;
            margin-top: -2px;
        }
        .logo .logo-icon {
            width: 36px;
            height: 36px;
            background: linear-gradient(135deg, var(--gold), var(--gold-dark));
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 1rem;
            color: #0a0d12;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 36px;
        }
        .nav-links a {
            font-size: 0.85rem;
            font-weight: 500;
            color: var(--text-secondary);
            transition: var(--transition);
            position: relative;
        }
        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gold);
            transition: var(--transition);
            border-radius: 10px;
        }
        .nav-links a:hover,
        .nav-links a.active {
            color: var(--text-primary);
        }
        .nav-links a:hover::before,
        .nav-links a.active::before {
            width: 100%;
        }

        .nav-cta {
            background: var(--gold);
            color: #0a0d12 !important;
            padding: 7px 22px;
            border-radius: 60px;
            font-weight: 700 !important;
            font-size: 0.8rem !important;
            transition: var(--transition);
        }
        .nav-cta::before {
            display: none !important;
        }
        .nav-cta:hover {
            transform: scale(1.05);
            background: #fff;
            color: #0a0d12 !important;
        }

        .hamburger {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            padding: 4px;
        }
        .hamburger span {
            width: 26px;
            height: 2px;
            background: var(--text-primary);
            border-radius: 10px;
            transition: var(--transition);
        }

        /* ============================================================
                   HERO — با تایپ‌رایتر و پارتیکل
                ============================================================ */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 50px 0 60px;
            position: relative;
            overflow: hidden;
        }

        /* پارتیکل‌های پس‌زمینه */
        #particles-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        .hero-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(212, 175, 55, 0.08);
            border: 1px solid rgba(212, 175, 55, 0.12);
            padding: 6px 22px;
            border-radius: 60px;
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--gold);
            letter-spacing: 0.5px;
            margin-bottom: 28px;
            backdrop-filter: blur(4px);
        }
        .hero-badge i {
            font-size: 0.8rem;
        }

        .hero h1 {
            font-size: 4rem;
            font-weight: 800;
            line-height: 1.08;
            margin-bottom: 8px;
        }
        .hero h1 .highlight {
            color: var(--gold);
            position: relative;
        }
        .hero h1 .highlight::after {
            content: '';
            position: absolute;
            bottom: 4px;
            left: 0;
            width: 100%;
            height: 10px;
            background: rgba(212, 175, 55, 0.15);
            border-radius: 10px;
            z-index: -1;
        }

        .hero .subtitle {
            font-size: 1.1rem;
            color: var(--text-secondary);
            max-width: 480px;
            margin: 12px 0 28px;
        }

        /* تایپ‌رایتر */
        .typewriter-wrap {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 28px;
            min-height: 40px;
        }
        .typewriter-wrap .static {
            color: var(--text-secondary);
        }
        .typewriter-wrap .dynamic {
            color: var(--gold);
            border-left: 3px solid var(--gold);
            padding-left: 6px;
            animation: blink 0.8s step-end infinite;
        }
        @keyframes blink {
            0%,
            100% {
                border-color: var(--gold);
            }
            50% {
                border-color: transparent;
            }
        }

        .hero-actions {
            display: flex;
            gap: 14px;
            flex-wrap: wrap;
        }

        .hero-stats {
            display: flex;
            gap: 48px;
            margin-top: 44px;
            padding-top: 32px;
            border-top: 1px solid rgba(255, 255, 255, 0.04);
        }
        .hero-stats .stat h3 {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--gold);
            letter-spacing: -0.5px;
        }
        .hero-stats .stat p {
            font-size: 0.75rem;
            color: var(--text-secondary);
            margin: 0;
            letter-spacing: 0.5px;
        }

        /* Hero Image */
        .hero-visual {
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .hero-ring {
            width: 380px;
            height: 380px;
            border-radius: 50%;
            background: conic-gradient(from 0deg, var(--gold), transparent 40%, var(--gold) 60%, transparent 80%, var(--gold));
            padding: 3px;
            animation: spinRing 20s linear infinite;
        }
        @keyframes spinRing {
            to {
                transform: rotate(360deg);
            }
        }

        .hero-ring .inner {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: var(--dark-bg);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 7rem;
            font-weight: 900;
            color: var(--gold);
            background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><text x="50" y="55" font-size="55" text-anchor="middle" dominant-baseline="central" fill="%23d4af37" font-weight="900">M</text></svg>');
            background-size: 55%;
            background-repeat: no-repeat;
            background-position: center;
            position: relative;
        }

        .floating-pill {
            position: absolute;
            background: rgba(14, 18, 27, 0.85);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(212, 175, 55, 0.1);
            padding: 10px 20px;
            border-radius: 60px;
            font-size: 0.75rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 12px 50px rgba(0, 0, 0, 0.4);
            animation: floatPill 5s ease-in-out infinite;
            color: var(--text-primary);
        }
        .floating-pill i {
            color: var(--gold);
            font-size: 1rem;
        }
        .fp-1 {
            top: 5%;
            right: -8%;
            animation-delay: 0s;
        }
        .fp-2 {
            bottom: 8%;
            left: -12%;
            animation-delay: 1.8s;
        }
        .fp-3 {
            bottom: 38%;
            right: -4%;
            animation-delay: 3.6s;
        }

        @keyframes floatPill {
            0%,
            100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-12px);
            }
        }

        /* ============================================================
                   SECTION COMMON
                ============================================================ */
        section {
            padding: 100px 0;
            position: relative;
        }

        .section-label {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(212, 175, 55, 0.06);
            border: 1px solid rgba(212, 175, 55, 0.06);
            padding: 4px 18px;
            border-radius: 60px;
            font-size: 0.7rem;
            font-weight: 600;
            color: var(--gold);
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 12px;
        }

        .section-title {
            font-size: 2.6rem;
            font-weight: 700;
            line-height: 1.15;
            margin-bottom: 10px;
        }
        .section-title .gold {
            color: var(--gold);
        }
        .section-desc {
            color: var(--text-secondary);
            font-size: 1.05rem;
            max-width: 560px;
            margin-bottom: 44px;
        }

        /* ============================================================
                   ABOUT — با کارت‌های آماری مدرن
                ============================================================ */
        .about {
            background: var(--dark-card);
            border-top: 1px solid var(--dark-border);
            border-bottom: 1px solid var(--dark-border);
        }

        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }

        .about-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin: 18px 0 24px;
        }
        .about-tags span {
            background: rgba(212, 175, 55, 0.06);
            border: 1px solid rgba(212, 175, 55, 0.06);
            padding: 5px 16px;
            border-radius: 60px;
            font-size: 0.75rem;
            color: var(--gold);
            font-weight: 500;
        }
        .about-tags span i {
            margin-left: 6px;
            font-size: 0.7rem;
        }

        .about-metrics {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
            margin-top: 28px;
        }
        .metric-card {
            background: var(--dark-bg);
            padding: 18px 16px;
            border-radius: 16px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.02);
            transition: var(--transition);
        }
        .metric-card:hover {
            border-color: rgba(212, 175, 55, 0.1);
            transform: translateY(-4px);
        }
        .metric-card h4 {
            font-size: 1.6rem;
            font-weight: 800;
            color: var(--gold);
        }
        .metric-card p {
            font-size: 0.7rem;
            color: var(--text-secondary);
            margin: 0;
        }

        .about-visual {
            background: linear-gradient(145deg, rgba(212, 175, 55, 0.03), rgba(212, 175, 55, 0.01));
            border-radius: var(--radius);
            padding: 36px;
            border: 1px solid rgba(212, 175, 55, 0.04);
        }
        .about-visual .code-block {
            background: var(--dark-bg);
            padding: 20px 24px;
            border-radius: 14px;
            font-family: 'Courier New', monospace;
            font-size: 0.8rem;
            color: var(--gold-light);
            border-right: 3px solid var(--gold);
            line-height: 1.8;
        }
        .about-visual .code-block .c {
            color: var(--text-secondary);
        }
        .about-visual .code-block .k {
            color: var(--gold);
        }
        .about-visual .code-block .s {
            color: #7ee8fa;
        }

        .about-partners {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-top: 18px;
        }
        .about-partners span {
            background: var(--dark-bg);
            padding: 5px 16px;
            border-radius: 60px;
            font-size: 0.7rem;
            color: var(--text-secondary);
            border: 1px solid rgba(255, 255, 255, 0.03);
        }
        .about-partners span i {
            color: var(--gold);
            margin-left: 6px;
        }

        /* ============================================================
                   STRATEGY — کارت‌های 3D
                ============================================================ */
        .strategy-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 28px;
        }

        .strategy-card {
            background: var(--dark-card);
            padding: 32px 28px;
            border-radius: var(--radius);
            border: 1px solid rgba(255, 255, 255, 0.02);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            cursor: default;
            transform-style: preserve-3d;
            perspective: 800px;
        }
        .strategy-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 30% 30%, rgba(212, 175, 55, 0.02), transparent 60%);
            opacity: 0;
            transition: var(--transition);
        }
        .strategy-card:hover::before {
            opacity: 1;
        }
        .strategy-card:hover {
            transform: translateY(-8px) rotateX(2deg);
            border-color: rgba(212, 175, 55, 0.12);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }
        .strategy-card .icon {
            font-size: 2.2rem;
            color: var(--gold);
            margin-bottom: 14px;
        }
        .strategy-card h4 {
            font-size: 1.1rem;
            margin-bottom: 6px;
        }
        .strategy-card p {
            color: var(--text-secondary);
            font-size: 0.85rem;
        }
        .strategy-card .tag {
            position: absolute;
            top: 16px;
            right: 16px;
            background: var(--gold);
            color: #0a0d12;
            font-weight: 800;
            font-size: 0.55rem;
            padding: 2px 14px;
            border-radius: 60px;
            letter-spacing: 0.5px;
        }

        /* ============================================================
                   ACHIEVEMENTS — تایم‌لاین عمودی
                ============================================================ */
        .achievements {
            background: var(--dark-card);
            border-top: 1px solid var(--dark-border);
            border-bottom: 1px solid var(--dark-border);
        }

        .timeline {
            position: relative;
            max-width: 800px;
            margin: 0 auto;
            padding-right: 40px;
        }
        .timeline::before {
            content: '';
            position: absolute;
            right: 8px;
            top: 0;
            bottom: 0;
            width: 2px;
            background: linear-gradient(to bottom, var(--gold), rgba(212, 175, 55, 0.1));
        }

        .timeline-item {
            position: relative;
            padding: 20px 28px;
            background: var(--dark-bg);
            border-radius: 16px;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.02);
            transition: var(--transition);
        }
        .timeline-item:hover {
            border-color: rgba(212, 175, 55, 0.08);
            transform: translateX(-6px);
        }
        .timeline-item::before {
            content: '';
            position: absolute;
            right: -34px;
            top: 28px;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: var(--gold);
            border: 3px solid var(--dark-bg);
            box-shadow: 0 0 0 4px rgba(212, 175, 55, 0.15);
        }
        .timeline-item .year {
            font-weight: 800;
            color: var(--gold);
            font-size: 0.9rem;
            display: inline-block;
            background: rgba(212, 175, 55, 0.06);
            padding: 1px 14px;
            border-radius: 60px;
            margin-bottom: 6px;
        }
        .timeline-item h4 {
            font-size: 1.05rem;
        }
        .timeline-item p {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin: 0;
        }

        /* ============================================================
                   TESTIMONIALS
                ============================================================ */
        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 28px;
        }
        .testimonial-card {
            background: var(--dark-card);
            padding: 32px;
            border-radius: var(--radius);
            border: 1px solid rgba(255, 255, 255, 0.02);
            transition: var(--transition);
        }
        .testimonial-card:hover {
            border-color: rgba(212, 175, 55, 0.06);
            transform: translateY(-4px);
        }
        .testimonial-card .stars {
            color: var(--gold);
            margin-bottom: 12px;
            font-size: 0.9rem;
        }
        .testimonial-card blockquote {
            color: var(--text-secondary);
            font-size: 0.95rem;
            font-style: italic;
            line-height: 1.8;
        }
        .testimonial-card .author {
            margin-top: 16px;
            font-weight: 600;
            color: var(--gold);
            font-size: 0.9rem;
        }
        .testimonial-card .author small {
            color: var(--text-secondary);
            font-weight: 400;
            font-size: 0.8rem;
        }

        /* ============================================================
                   CTA
                ============================================================ */
        .cta {
            background: linear-gradient(180deg, var(--dark-bg), rgba(212, 175, 55, 0.02));
            border-top: 1px solid var(--dark-border);
            border-bottom: 1px solid var(--dark-border);
            text-align: center;
        }
        .cta h2 {
            font-size: 2.8rem;
            font-weight: 700;
        }
        .cta h2 span {
            color: var(--gold);
        }
        .cta p {
            color: var(--text-secondary);
            max-width: 480px;
            margin: 14px auto 32px;
        }
        .cta .btn-group {
            display: flex;
            gap: 14px;
            justify-content: center;
            flex-wrap: wrap;
        }

        /* ============================================================
                   FOOTER
                ============================================================ */
        footer {
            padding: 32px 0;
            border-top: 1px solid var(--dark-border);
        }
        .footer-grid {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }
        .footer-grid .brand {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .footer-grid .brand .logo-icon {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, var(--gold), var(--gold-dark));
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 0.9rem;
            color: #0a0d12;
        }
        .footer-grid .brand .name {
            font-weight: 700;
            font-size: 1rem;
        }
        .footer-grid .brand .sub {
            font-size: 0.65rem;
            color: var(--text-secondary);
            display: block;
        }
        .footer-socials {
            display: flex;
            gap: 16px;
        }
        .footer-socials a {
            color: var(--text-secondary);
            font-size: 1.2rem;
            transition: var(--transition);
        }
        .footer-socials a:hover {
            color: var(--gold);
            transform: translateY(-3px);
        }
        .footer-copy {
            color: var(--text-muted);
            font-size: 0.8rem;
        }

        /* ============================================================
                   RESPONSIVE
                ============================================================ */
        @media (max-width: 1024px) {
            .hero h1 {
                font-size: 3.2rem;
            }
            .hero-ring {
                width: 300px;
                height: 300px;
            }
            .fp-1,
            .fp-2,
            .fp-3 {
                display: none;
            }
        }

        @media (max-width: 900px) {
            .hero-grid {
                grid-template-columns: 1fr;
                text-align: center;
            }
            .hero .subtitle {
                margin: 12px auto 20px;
            }
            .typewriter-wrap {
                justify-content: center;
            }
            .hero-actions {
                justify-content: center;
            }
            .hero-stats {
                justify-content: center;
            }
            .about-grid {
                grid-template-columns: 1fr;
            }
            .about-metrics {
                grid-template-columns: repeat(3, 1fr);
            }
            .hero-ring {
                width: 220px;
                height: 220px;
            }
            .hero-ring .inner {
                font-size: 4rem;
            }
            .section-title {
                font-size: 2.1rem;
            }
        }

        @media (max-width: 768px) {
            .nav-links {
                display: none;
                flex-direction: column;
                background: rgba(7, 10, 15, 0.98);
                backdrop-filter: blur(32px);
                position: absolute;
                top: 64px;
                left: 0;
                width: 100%;
                padding: 28px 24px;
                gap: 18px;
                border-bottom: 1px solid rgba(212, 175, 55, 0.06);
            }
            .nav-links.open {
                display: flex;
            }
            .hamburger {
                display: flex;
            }

            .hero {
                padding: 30px 0 40px;
            }
            .hero h1 {
                font-size: 2.2rem;
            }
            .hero-stats {
                flex-direction: column;
                gap: 8px;
                padding-top: 20px;
            }
            .hero-stats .stat h3 {
                font-size: 1.4rem;
            }

            .about-metrics {
                grid-template-columns: 1fr 1fr;
            }
            .timeline {
                padding-right: 24px;
            }
            .timeline-item::before {
                right: -28px;
                width: 10px;
                height: 10px;
            }
            .timeline-item {
                padding: 16px 20px;
            }
            .cta h2 {
                font-size: 2rem;
            }
            .footer-grid {
                flex-direction: column;
                text-align: center;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 1.8rem;
            }
            .about-metrics {
                grid-template-columns: 1fr;
            }
            .strategy-grid {
                grid-template-columns: 1fr;
            }
            .testimonial-grid {
                grid-template-columns: 1fr;
            }
            .hero-ring {
                width: 170px;
                height: 170px;
            }
            .hero-ring .inner {
                font-size: 3rem;
            }
            .btn-gold,
            .btn-outline {
                padding: 12px 24px;
                font-size: 0.8rem;
            }
        }

        /* ============================================================
                   REVEAL ANIMATIONS
                ============================================================ */
        .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.9s cubic-bezier(0.23, 1, 0.32, 1);
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
        .reveal-left {
            opacity: 0;
            transform: translateX(-40px);
            transition: all 0.9s cubic-bezier(0.23, 1, 0.32, 1);
        }
        .reveal-left.active {
            opacity: 1;
            transform: translateX(0);
        }
        .reveal-right {
            opacity: 0;
            transform: translateX(40px);
            transition: all 0.9s cubic-bezier(0.23, 1, 0.32, 1);
        }
        .reveal-right.active {
            opacity: 1;
            transform: translateX(0);
        }
    </style>
</head>
<body>

    <!-- ============================================================
    HEADER
    ============================================================ -->
    <header id="header">
        <div class="container nav">
            <div class="logo">
                <div class="logo-icon">M</div>
                <div>
                    Metifx<span class="gold">.</span>
                    <span class="sub">مهدی عرفانیان</span>
                </div>
            </div>
            <ul class="nav-links" id="navLinks">
                <li><a href="#about" class="active">درباره</a></li>
                <li><a href="#market">بازار زنده</a></li>
                <li><a href="#strategy">استراتژی</a></li>
                <li><a href="#services">خدمات</a></li>
                <li><a href="#achievements">افتخارات</a></li>
                <li><a href="#testimonials">نظرات</a></li>
                <li><a href="#contact" class="nav-cta">ارتباط</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span><span></span><span></span>
            </div>
        </div>
    </header>

    <!-- ============================================================
    LIVE TICKER
    ============================================================ -->
    <div style="padding-top:64px;">
        <div class="tradingview-widget-container" id="tickerWidget">
            <div class="tradingview-widget-container__widget"></div>
            <script type="text/javascript" src="https://s3.tradingview.com/external-embedding/embed-widget-ticker-tape.js" async>
            {
              "symbols": [
                { "proName": "OANDA:XAUUSD", "title": "طلا XAUUSD" },
                { "proName": "BITSTAMP:BTCUSD", "title": "بیت‌کوین" },
                { "proName": "OANDA:XAGUSD", "title": "نقره XAGUSD" },
                { "proName": "FX:EURUSD", "title": "EUR/USD" },
                { "proName": "FX:GBPUSD", "title": "GBP/USD" },
                { "proName": "TVC:DXY", "title": "شاخص دلار" },
                { "proName": "CAPITALCOM:US30", "title": "داوجونز" }
              ],
              "showSymbolLogo": true,
              "isTransparent": true,
              "displayMode": "adaptive",
              "colorTheme": "dark",
              "locale": "en"
            }
            </script>
        </div>
        <div id="tickerFallback" style="display:none; text-align:center; padding:10px; font-size:0.75rem; color:var(--text-muted); background:var(--dark-card); border-bottom:1px solid var(--dark-border);">
            داده لحظه‌ای بازار بارگذاری نشد — ممکن است دسترسی به سرور قیمت‌ها نیاز به VPN داشته باشد.
        </div>
    </div>

    <!-- ============================================================
    HERO
    ============================================================ -->
    <section class="hero" id="home">
        <canvas id="particles-canvas"></canvas>

        <div class="container hero-grid">
            <div class="hero-text">
                <div class="hero-badge">
                    <i class="fas fa-crown"></i> ۱۰۰ تریدر برتر ایران
                </div>

                <h1>
                    مهدی <span class="highlight">عرفانیان</span>
                </h1>

                <div class="typewriter-wrap">
                    <span class="static">متخصص</span>
                    <span class="dynamic" id="typewriter"></span>
                </div>

                <p class="subtitle">
                    بنیان‌گذار استراتژی <strong style="color:var(--gold);">MRN</strong> با ۴ سال سابقه در بازارهای مالی،
                    همکاری با پراپ‌های بین‌المللی و تحلیل تخصصی طلا (XAUUSD).
                </p>

                <div class="hero-actions">
                    <a href="#contact" class="btn-gold"><i class="fas fa-paper-plane"></i> همکاری حرفه‌ای</a>
                    <a href="#strategy" class="btn-outline"><i class="fas fa-chart-line"></i> استراتژی MRN</a>
                </div>

                <div class="hero-stats">
                    <div class="stat">
                        <h3>۴+</h3>
                        <p>سال سابقه</p>
                    </div>
                    <div class="stat">
                        <h3>۱۵+</h3>
                        <p>پراپ شراکت</p>
                    </div>
                    <div class="stat">
                        <h3>۹۸%</h3>
                        <p>دقت تحلیل</p>
                    </div>
                </div>
            </div>

            <div class="hero-visual">
                <div class="hero-ring">
                    <div class="inner"></div>
                </div>
                <div class="floating-pill fp-1">
                    <i class="fas fa-trophy"></i> رتبه برتر
                </div>
                <div class="floating-pill fp-2">
                    <i class="fas fa-code"></i> MRN Strategy
                </div>
                <div class="floating-pill fp-3">
                    <i class="fas fa-gem"></i> XAUUSD Pro
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    ABOUT
    ============================================================ -->
    <section class="about" id="about">
        <div class="container">
            <div class="about-grid">
                <div class="about-text reveal-left">
                    <span class="section-label"><i class="fas fa-user-astronaut"></i> بیوگرافی</span>
                    <h2 class="section-title">مهدی عرفانیان <span class="gold">کیست؟</span></h2>
                    <p style="color:var(--text-secondary); font-size:1.05rem;">
                        متولد <strong style="color:var(--gold);">خوی</strong>، ۲۱ ساله. از ۱۷ سالگی وارد دنیای ترید شد
                        و در کمتر از ۴ سال با تکیه بر تحلیل تکنیکال و روانشناسی بازار، جایگاه خود را در میان
                        نخبگان ترید ایران تثبیت کرد.
                    </p>

                    <div class="about-tags">
                        <span><i class="fas fa-map-pin"></i> خوی، ایران</span>
                        <span><i class="fas fa-calendar"></i> ۲۱ سال</span>
                        <span><i class="fas fa-bolt"></i> متخصص XAUUSD</span>
                        <span><i class="fas fa-flag"></i> بنیان‌گذار MRN</span>
                    </div>

                    <p style="color:var(--text-secondary);">
                        استراتژی <strong style="color:var(--gold);">MRN</strong> (Market Structure Nesting & Reaction)
                        نگاه جدیدی به ساپلای و دیماند در بازار طلا ارائه داده است. هم‌اکنون با چندین پراپ‌فرم
                        معتبر همکاری داشته و به عنوان منتور و تحلیل‌گر ارشد فعالیت می‌کند.
                    </p>

                    <div class="about-metrics">
                        <div class="metric-card">
                            <h4>۱۵۰۰+</h4>
                            <p>تحلیل روزانه</p>
                        </div>
                        <div class="metric-card">
                            <h4>۹۲%</h4>
                            <p>نرخ برد ستاپ‌های تاییدشده*</p>
                        </div>
                        <div class="metric-card">
                            <h4>۴۰+</h4>
                            <p>تریدر منتور</p>
                        </div>
                    </div>
                    <p style="color:var(--text-muted); font-size:0.72rem; margin-top:14px;">*بر اساس بک‌تست و عملکرد ستاپ‌های تعریف‌شده در استراتژی MRN؛ تضمینی برای نتایج آینده نیست.</p>
                </div>

                <div class="about-visual reveal-right">
                    <div class="code-block">
                        <span class="c">// هسته استراتژی MRN</span><br>
                        <span class="k">MRN</span> = {<br>
                        &nbsp;&nbsp;MarketStructure: <span class="s">'Nesting'</span>,<br>
                        &nbsp;&nbsp;Reaction: <span class="s">'Supply & Demand'</span>,<br>
                        &nbsp;&nbsp;Asset: <span class="s">'XAUUSD'</span>,<br>
                        &nbsp;&nbsp;WinRate: <span class="s">'92%'</span><br>
                        };
                    </div>
                    <div class="about-partners">
                        <span><i class="fas fa-check-circle"></i> FTMO</span>
                        <span><i class="fas fa-check-circle"></i> The Funded</span>
                        <span><i class="fas fa-check-circle"></i> Funding Pips</span>
                        <span><i class="fas fa-check-circle"></i> Topstep</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    LIVE CHART
    ============================================================ -->
    <section class="strategy" id="market">
        <div class="container">
            <span class="section-label"><i class="fas fa-chart-line"></i> بازار زنده</span>
            <h2 class="section-title">چارت زنده <span class="gold">XAUUSD</span></h2>
            <p class="section-desc">
                حرکت لحظه‌ای طلا؛ همان چارتی که تحلیل‌های MRN روی آن انجام می‌شود.
            </p>

            <div style="background:var(--dark-card); border:1px solid var(--dark-border); border-radius:var(--radius); box-shadow:var(--shadow-gold); overflow:hidden; height:520px; position:relative;">
                <div id="chartFallback" style="display:none; position:absolute; inset:0; align-items:center; justify-content:center; flex-direction:column; gap:10px; text-align:center; padding:20px; color:var(--text-muted); font-size:0.85rem;">
                    <i class="fas fa-chart-line" style="font-size:1.6rem; color:var(--gold);"></i>
                    چارت زنده بارگذاری نشد — احتمالاً به دلیل محدودیت دسترسی به سرور TradingView است؛ با VPN دوباره امتحان کنید.
                </div>
                <div class="tradingview-widget-container" id="chartWidget" style="height:100%;">
                    <div class="tradingview-widget-container__widget" style="height:100%;"></div>
                    <script type="text/javascript" src="https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js" async>
                    {
                      "autosize": true,
                      "symbol": "OANDA:XAUUSD",
                      "interval": "60",
                      "timezone": "Asia/Tehran",
                      "theme": "dark",
                      "style": "1",
                      "locale": "en",
                      "backgroundColor": "rgba(14, 18, 27, 1)",
                      "gridColor": "rgba(212, 175, 55, 0.06)",
                      "hide_top_toolbar": false,
                      "hide_legend": false,
                      "allow_symbol_change": true,
                      "support_host": "https://www.tradingview.com"
                    }
                    </script>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    STRATEGY
    ============================================================ -->
    <section class="strategy" id="strategy">
        <div class="container">
            <span class="section-label"><i class="fas fa-cogs"></i> هسته اصلی</span>
            <h2 class="section-title">استراتژی <span class="gold">MRN</span></h2>
            <p class="section-desc">
                روشی نوین در تحلیل ساختار بازار و واکنش‌های قیمتی، ویژه معاملات طلا (XAUUSD).
            </p>

            <div class="strategy-grid">
                <div class="strategy-card reveal">
                    <span class="tag">MRN</span>
                    <div class="icon"><i class="fas fa-layer-group"></i></div>
                    <h4>ساختار آشیانی</h4>
                    <p>شناسایی سلسله‌مراتب ساختار بازار از تایم‌فریم بالا به پایین برای یافتن نقاط کلیدی.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-bullseye"></i></div>
                    <h4>واکنش به نواحی</h4>
                    <p>دقیق‌ترین نقاط ورود و خروج بر اساس برخورد قیمت به نواحی عرضه و تقاضای معتبر.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-coins"></i></div>
                    <h4>تخصص طلا</h4>
                    <p>بهینه‌سازی پارامترهای استراتژی برای جفت‌ارز طلا با توجه به نوسانات و رفتار ویژه آن.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-brain"></i></div>
                    <h4>روانشناسی بازار</h4>
                    <p>تلفیق تحلیل ساختار با روانشناسی جمعی تریدرها برای پیش‌بینی حرکت‌های بزرگ.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    SERVICES
    ============================================================ -->
    <section class="strategy" id="services" style="background:var(--dark-card);">
        <div class="container">
            <span class="section-label"><i class="fas fa-briefcase"></i> همکاری</span>
            <h2 class="section-title">خدمات <span class="gold">قابل ارائه</span></h2>
            <p class="section-desc">
                زمینه‌های همکاری با پراپ‌فرم‌ها، تریدرهای مستقل و تیم‌های سرمایه‌گذاری.
            </p>

            <div class="strategy-grid">
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-user-graduate"></i></div>
                    <h4>منتورینگ خصوصی</h4>
                    <p>آموزش ساختاریافته استراتژی MRN برای تریدرهای مبتدی تا حرفه‌ای، به‌صورت جلسات خصوصی.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-chart-area"></i></div>
                    <h4>تحلیل اختصاصی XAUUSD</h4>
                    <p>تحلیل روزانه ساختار بازار طلا بر پایه استراتژی MRN برای تیم‌ها و سرمایه‌گذاران.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-handshake"></i></div>
                    <h4>همکاری با پراپ‌فرم‌ها</h4>
                    <p>مشاوره و اجرای معاملات در قالب همکاری رسمی با پراپ‌فرم‌های بین‌المللی.</p>
                </div>
                <div class="strategy-card reveal">
                    <div class="icon"><i class="fas fa-shield-alt"></i></div>
                    <h4>مدیریت ریسک</h4>
                    <p>طراحی چارچوب مدیریت سرمایه و ریسک متناسب با حساب و اهداف هر تریدر.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    ACHIEVEMENTS
    ============================================================ -->
    <section class="achievements" id="achievements">
        <div class="container">
            <span class="section-label"><i class="fas fa-medal"></i> کارنامه</span>
            <h2 class="section-title">افتخارات &amp; <span class="gold">دستاوردها</span></h2>
            <p class="section-desc">مسیری که مهدی عرفانیان در بازارهای مالی پیموده است.</p>

            <div class="timeline">
                <div class="timeline-item reveal">
                    <div class="year">۱۴۰۱</div>
                    <h4>شروع فعالیت حرفه‌ای</h4>
                    <p>ورود به بازار فارکس با تمرکز ویژه بر طلا و توسعه اولیه استراتژی MRN.</p>
                </div>
                <div class="timeline-item reveal">
                    <div class="year">۱۴۰۲</div>
                    <h4>اولین همکاری با پراپ</h4>
                    <p>اخذ چالش FTMO و موفقیت در آن، آغاز همکاری با چندین پراپ‌فرم بین‌المللی.</p>
                </div>
                <div class="timeline-item reveal">
                    <div class="year">۱۴۰۳</div>
                    <h4>دو دوره رتبه‌آوری در مسابقه هاوس آف ترید</h4>
                    <p>رتبه ۸۱ از میان ۳۵,۰۰۰ شرکت‌کننده (بستر Ahangari Holding) و رتبه ۴۱۰ از میان ۶,۰۵۱ شرکت‌کننده (بستر FENEFX) در دو دوره مسابقه «House of Trade».</p>
                </div>
                <div class="timeline-item reveal">
                    <div class="year">۱۴۰۴</div>
                    <h4>بنیان‌گذاری رسمی MRN</h4>
                    <p>ارائه نسخه نهایی استراتژی MRN و منتورینگ بیش از ۴۰ تریدر حرفه‌ای.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    TESTIMONIALS
    ============================================================ -->
    <section class="testimonials" id="testimonials">
        <div class="container">
            <span class="section-label"><i class="fas fa-quote-right"></i> هم‌نظران</span>
            <h2 class="section-title">نظر <span class="gold">تریدرها</span></h2>
            <p class="section-desc">آنچه همکاران و تریدرهای حرفه‌ای درباره مهدی می‌گویند.</p>

            <div class="testimonial-grid">
                <div class="testimonial-card reveal">
                    <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div>
                    <blockquote>"استراتژی MRN نگاه من به بازار طلا را کاملاً متحول کرد. دقت تحلیل‌های مهدی بی‌نظیره."</blockquote>
                    <div class="author">محمد رضایی <small>تریدر حرفه‌ای</small></div>
                </div>
                <div class="testimonial-card reveal">
                    <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div>
                    <blockquote>"با وجود سن کم، مهدی یکی از بااستعدادترین تحلیل‌گرانی هست که دیدم. روانشناسی بازار رو عالی بلده."</blockquote>
                    <div class="author">سارا کریمی <small>تحلیل‌گر ارشد</small></div>
                </div>
                <div class="testimonial-card reveal">
                    <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div>
                    <blockquote>"همکاری با مهدی در پروژه‌های پراپ، یکی از بهترین تجربه‌های کاری من بود. نتیجه‌گرا و فوق‌حرفه‌ای."</blockquote>
                    <div class="author">علی نادری <small>مدیر ریسک</small></div>
                </div>
            </div>
        </div>
    </section>

    <!-- ============================================================
    CTA
    ============================================================ -->
    <section class="cta" id="contact">
        <div class="container">
            <h2>همکاری <span>حرفه‌ای</span> با مهدی</h2>
            <p>برای مشاوره، منتورینگ، یا همکاری در پروژه‌های تحلیل و پراپ، با ما در ارتباط باشید.</p>
            <div class="btn-group">
                <a href="https://t.me/MeTiTr" target="_blank" rel="noopener" class="btn-gold"><i class="fab fa-telegram"></i> تلگرام</a>
                <a href="https://www.instagram.com/mahdi_erfaniannn" target="_blank" rel="noopener" class="btn-gold" style="background:linear-gradient(135deg,#f58529,#dd2a7b,#8134af); box-shadow:0 8px 40px rgba(221,42,123,0.2);"><i class="fab fa-instagram"></i> اینستاگرام</a>
                <a href="mailto:erfanianmahdi750@gmail.com" class="btn-outline"><i class="fas fa-envelope"></i> ایمیل</a>
            </div>
        </div>
    </section>

    <!-- ============================================================
    FOOTER
    ============================================================ -->
    <footer>
        <div class="container footer-grid">
            <div class="brand">
                <div class="logo-icon">M</div>
                <div>
                    <div class="name">Metifx<span style="color:var(--gold);">.</span></div>
                    <span class="sub">مهدی عرفانیان | بنیان‌گذار MRN</span>
                </div>
            </div>
            <div class="footer-socials">
                <a href="https://t.me/MeTiTr" target="_blank" rel="noopener"><i class="fab fa-telegram"></i></a>
                <a href="https://www.instagram.com/mahdi_erfaniannn" target="_blank" rel="noopener"><i class="fab fa-instagram"></i></a>
                <a href="mailto:erfanianmahdi750@gmail.com"><i class="fas fa-envelope"></i></a>
            </div>
            <span class="footer-copy">© ۱۴۰۴ تمامی حقوق محفوظ است.</span>
        </div>
        <div class="container">
            <p style="margin-top:20px; padding-top:20px; border-top:1px solid var(--dark-border); color:var(--text-muted); font-size:0.72rem; line-height:1.9; max-width:900px;">
                معاملات در بازارهای مالی با ریسک همراه است و عملکرد گذشته تضمینی برای نتایج آینده نیست. اطلاعات این وب‌سایت صرفاً جنبه آموزشی و تحلیلی دارد و توصیه سرمایه‌گذاری محسوب نمی‌شود.
            </p>
        </div>
    </footer>

    <!-- ============================================================
    SCRIPTS
    ============================================================ -->
    <script>
        // ============================================================
        // 1. HAMBURGER
        // ============================================================
        const hamburger = document.getElementById('hamburger');
        const navLinks = document.getElementById('navLinks');

        hamburger.addEventListener('click', () => {
            navLinks.classList.toggle('open');
        });

        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('open');
            });
        });

        // ============================================================
        // 2. HEADER SCROLL
        // ============================================================
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            header.classList.toggle('scrolled', window.scrollY > 50);
        });

        // ============================================================
        // 3. ACTIVE NAV LINK
        // ============================================================
        const sections = document.querySelectorAll('section[id]');
        const navItems = document.querySelectorAll('.nav-links a:not(.nav-cta)');

        window.addEventListener('scroll', () => {
            let current = '';
            sections.forEach(section => {
                const top = section.offsetTop - 140;
                if (window.scrollY >= top) {
                    current = section.getAttribute('id');
                }
            });
            navItems.forEach(item => {
                item.classList.toggle('active', item.getAttribute('href') === '#' + current);
            });
        });

        // ============================================================
        // 4. REVEAL ON SCROLL
        // ============================================================
        const revealEls = document.querySelectorAll('.reveal, .reveal-left, .reveal-right');

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                }
            });
        }, {
            threshold: 0.12,
            rootMargin: '0px 0px -30px 0px'
        });

        revealEls.forEach(el => observer.observe(el));

        // ============================================================
        // 5. SMOOTH SCROLL
        // ============================================================
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                const target = this.getAttribute('href');
                if (target === '#') return;
                e.preventDefault();
                const el = document.querySelector(target);
                if (el) {
                    window.scrollTo({
                        top: el.offsetTop - 70,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // ============================================================
        // 6. TYPEWRITER EFFECT
        // ============================================================
        const words = ['XAUUSD', 'تحلیل طلا', 'استراتژی MRN', 'Market Structure', 'Supply & Demand'];
        let wordIndex = 0;
        let charIndex = 0;
        let isDeleting = false;
        const typewriterEl = document.getElementById('typewriter');

        function typeEffect() {
            const currentWord = words[wordIndex];

            if (isDeleting) {
                typewriterEl.textContent = currentWord.substring(0, charIndex - 1);
                charIndex--;
            } else {
                typewriterEl.textContent = currentWord.substring(0, charIndex + 1);
                charIndex++;
            }

            let speed = isDeleting ? 60 : 100;

            if (!isDeleting && charIndex === currentWord.length) {
                speed = 2000;
                isDeleting = true;
            } else if (isDeleting && charIndex === 0) {
                isDeleting = false;
                wordIndex = (wordIndex + 1) % words.length;
                speed = 400;
            }

            setTimeout(typeEffect, speed);
        }

        typeEffect();

        // ============================================================
        // 7. PARTICLES CANVAS
        // ============================================================
        const canvas = document.getElementById('particles-canvas');
        const ctx = canvas.getContext('2d');
        let width, height;
        let particles = [];

        function resize() {
            const rect = canvas.parentElement.getBoundingClientRect();
            width = canvas.width = rect.width;
            height = canvas.height = rect.height;
        }

        class Particle {
            constructor() {
                this.x = Math.random() * width;
                this.y = Math.random() * height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = (Math.random() - 0.5) * 0.3;
                this.speedY = (Math.random() - 0.5) * 0.3;
                this.opacity = Math.random() * 0.3 + 0.1;
            }

            update() {
                this.x += this.speedX;
                this.y += this.speedY;

                if (this.x < 0 || this.x > width) this.speedX *= -1;
                if (this.y < 0 || this.y > height) this.speedY *= -1;
            }

            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(212, 175, 55, ${this.opacity})`;
                ctx.fill();
            }
        }

        function initParticles() {
            particles = [];
            const count = Math.min(80, Math.floor((width * height) / 8000));
            for (let i = 0; i < count; i++) {
                particles.push(new Particle());
            }
        }

        function animateParticles() {
            ctx.clearRect(0, 0, width, height);
            particles.forEach(p => {
                p.update();
                p.draw();
            });

            // draw lines
            for (let i = 0; i < particles.length; i++) {
                for (let j = i + 1; j < particles.length; j++) {
                    const dx = particles[i].x - particles[j].x;
                    const dy = particles[i].y - particles[j].y;
                    const dist = Math.sqrt(dx * dx + dy * dy);
                    if (dist < 120) {
                        ctx.beginPath();
                        ctx.moveTo(particles[i].x, particles[i].y);
                        ctx.lineTo(particles[j].x, particles[j].y);
                        ctx.strokeStyle = `rgba(212, 175, 55, ${0.04 * (1 - dist / 120)})`;
                        ctx.lineWidth = 0.5;
                        ctx.stroke();
                    }
                }
            }
            requestAnimationFrame(animateParticles);
        }

        window.addEventListener('resize', () => {
            resize();
            initParticles();
        });

        resize();
        initParticles();
        animateParticles();

        // ============================================================
        // 8. CONSOLE EASTER EGG
        // ============================================================
        console.log('%c Metifx | مهدی عرفانیان ', 'background:#d4af37; color:#0a0d12; font-size:20px; font-weight:bold; padding:12px 24px; border-radius:8px;');
        console.log('%c بنیان‌گذار استراتژی MRN | ۱۰۰ تریدر برتر ایران ', 'color:#d4af37; font-size:14px;');

        // ============================================================
        // 9. TRADINGVIEW WIDGET FALLBACK
        // اگر ویجت‌های TradingView (مثلاً به‌دلیل محدودیت دسترسی) لود نشوند،
        // بعد از چند ثانیه پیام جایگزین نشان داده می‌شود.
        // ============================================================
        function checkWidgetLoaded(containerId, fallbackId, displayMode) {
            setTimeout(() => {
                const container = document.getElementById(containerId);
                const fallback = document.getElementById(fallbackId);
                if (!container || !fallback) return;
                const widget = container.querySelector('iframe');
                if (!widget) {
                    fallback.style.display = displayMode;
                }
            }, 5000);
        }
        checkWidgetLoaded('tickerWidget', 'tickerFallback', 'block');
        checkWidgetLoaded('chartWidget', 'chartFallback', 'flex');
    </script>

</body>
</html>
